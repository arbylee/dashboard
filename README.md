# Store google api secrets

`./bin/store_secret.js`

Enter a password that you will use to store encrypted credentials

Store with the name `google`

# Run the server

node ./server/server.js

You will need to enter your password on startup
